# Server-side only — used by the Next.js BFF proxy to reach FastAPI internally.
# In Railway, set this to http://backend.railway.internal:8000
# Never expose this as NEXT_PUBLIC_ — the browser should never talk to FastAPI directly.
BACKEND_URL=http://localhost:8000
